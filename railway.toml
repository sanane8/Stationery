[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bash start_railway.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PYTHON_VERSION = "3.11"
DJANGO_SETTINGS_MODULE = "stationery_tracker.production_settings"
