[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py runserver 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PYTHON_VERSION = "3.11"
DATABASE_URL = "postgresql://postgres:railway@localhost:5432/railway"
